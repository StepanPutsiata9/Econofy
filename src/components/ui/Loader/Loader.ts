import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  spinner: {
    width: 50,
    height: 50,
    borderRadius: 30,
    borderWidth: 5,
    borderColor: '#5BFF6F',
    borderTopColor: 'transparent',
  },
});
